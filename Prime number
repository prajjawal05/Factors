#include<stdio.h>
#include<stdlib.h>
void main()
{
	int flag=0,i,n;
	scanf("%d", &n);
	for(i=2;i<n;i++)
	{
		if(n%i==0)
		{
			flag=1;
			break;
		}
	}
	if(flag==0)
		printf("yES");
	else
		printf("No");
}

